# Usamos esse arquivo para importar as views dentro do package pai deste arquivo, no caso: views
from .contact_views import *  # Usar o ponto (.) antes do arquivo, para indicar que o arquivo está na raiz do mesmo diretorio do __init__
from .contact_forms import *